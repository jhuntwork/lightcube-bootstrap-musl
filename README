These build scripts will create an LFS-style dev environment from which rpm packages for the
LightCube OS base system can be built.

To begin building modify 'Makefile' to meet your specific needs and issue 'make'.
